---
// src/pages/index.astro
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// ১. Astro = await response.json();

            if (data && data.results && data.results.length > 0) {
               Content Collections থেকে ডাটা লোড
let hotels = [];
try {
  const collectionData = await getCollection('hotels');
  if (collectionData && collectionData.length > 0) {
    hotels = collectionData.map((item: any) => ({
      slug: item.id?.replace(/\.md$/, '')const suppliersList = [];

              data.results.forEach(item => {
                const itemName = (item.name || "").toLowerCase();
                 || item.slug || 'hotel',
      title: item.data?.title || item.data?.name || 'Gulshan Hotel',
      const targetName = hotelName.toLowerCase();

                if (itemName.includes(targetName.substring(0,category: item.data?.category || 'Luxury Hotel',
      location: item.data?.location || 'Gulshan 2, Dhaka',
      area: (item.data?.location || 'Gulshan 2').toLowerCase 5)) || targetName.includes(itemName.substring(0, 5))) {
                  if (item().includes('gulshan 1') ? 'gulshan-1' : (item.data?.location || '')..suppliers) {
                    Object.keys(item.suppliers).forEach(key => {
                      const supp =toLowerCase().includes('banani') ? 'banani' : 'gulshan-2',
      price: item.data?.price || '$$$$',
      rating: item.data?.rating || 4.8, item.suppliers[key];
                      if (supp && supp.link) {
                        suppliersList.push({
      reviews: item.data?.reviewsCount || '1,248',
      description: item.
                          key: key,
                          name: key.toUpperCase() === 'HOTELSCOM' ? 'Hotelsdata?.description || 'Premium hotel featuring elegant rooms, rooftop swimming pool, fine dining, and diplomatic security in Gulshan.',
      image: item.data?.image || 'https://images.unsplash.com/photo-15.com' : key.charAt(0).toUpperCase() + key.slice(1) + '.com',
66073771259-6a8506099945?auto=format&fit=crop&w=800&q=80',
    }));
  }
} catch (err) {
  hotels = [];
}

// ২. কালেকশন ফাঁ                          link: supp.link,
                          logo: supp.media?.logoSquare || null,
                          totalPrice: parsePriceNumber(supp)কা থাকলে Relative Glob দিয়ে ফাইল স্ক্যান
if (!hotels || hotels.length === 0) {
  try
                        });
                      }
                    });
                  }
                }
              });

              suppliersList.sort {
    const rawGlob = import.meta.glob('../content/hotels/*.md', { eager: true });
    const globList = Object.entries(rawGlob);
    if (globList && globList.length((a, b) => (a.totalPrice && b.totalPrice ? a.totalPrice - b.totalPrice : > 0) {
      hotels = globList.map(([filePath, item]: [string, any]) => {
        const filename = filePath.split('/').pop()?.replace('.md', '') || 'hotel';
        return {
          slug: 0));

              if (suppliersList.length > 0) {
                otaContainer.innerHTML = ''; filename,
          title: item.frontmatter?.title || item.frontmatter?.name || 'Gulshan Hotel

                suppliersList.forEach((supp, index) => {
                  const isLowest = index === 0 &&',
          category: item.frontmatter?.category || 'Luxury Hotel',
          location: item.frontmatter?.location || 'Gulshan 2, Dhaka',
          area: (item.frontmatter?.location || ' supp.totalPrice;
                  const basePriceNum = supp.totalPrice ? Math.round(supp.totalGulshan 2').toLowerCase().includes('gulshan 1') ? 'gulshan-1' : (item.frontmatter?.location || '').toLowerCase().includes('banani') ? 'banani' : 'gulshan-Price / 1.15) : null;
                  const priceDisplay = basePriceNum ? `$${base2',
          price: item.frontmatter?.price || '$$$$',
          rating: item.frontmatter?.rating || 4.PriceNum}` : `Check rate`;

                  const buttonClass = isLowest
                    ? `bg-8,
          reviews: item.frontmatter?.reviewsCount || '1,248',
          description: item.frontmatter[#00e676] hover:bg-[#00c853] text-slate-900 font?.description || 'Premium hotel featuring elegant rooms, rooftop swimming pool, fine dining, and diplomatic security in Gulshan.',
          image: item.frontmatter?.image || 'https://images.unsplash.com/photo-156607377-extrabold px-4 py-2 rounded-full transition shadow-xs text-xs`
                    :1259-6a8506099945?auto=format&fit= `border border-slate-900 hover:bg-slate-900 hover:text-white textcrop&w=800&q=80',
        };
      });
    }
  } catch (err) {
    hotels = [];
  }
}

// ৩. সেফটি ডি-slate-900 font-bold px-4 py-2 rounded-full transition text-xs`;

ফল্ট ডাটা
if (!hotels || hotels.length === 0) {
  hotels =                  const logoHtml = supp.logo 
                    ? `<img src="${supp.logo}" alt="${supp.name}" class="h-5 sm:h-6 object-contain" />` 
                    : `<span [
    {
      slug: "the-westin-dhaka",
      title: "The Westin Dhaka",
      category: "Luxury Hotel",
      location: "Gulshan 2, Dhaka",
      area: "gulshan-2",
      price: "৳ 18,500 / night",
      rating: 4.7,
      reviews: "1,248",
      description: "Premium business hotel class="font-extrabold text-slate-900 text-xs sm:text-sm">${supp with elegant rooms, world-class dining and excellent meeting facilities in Gulshan 2.",
      image: "https.name}</span>`;

                  const cardHTML = `
                    <div class="bg-white rounded-xl://images.unsplash.com/photo-1566073771259-6a8506099945?auto=format&fit=crop&w=80 border border-slate-200 p-3 sm:p-3.5 flex items-center justify-0&q=80",
    },
    {
      slug: "amari-dhaka",between gap-2 shadow-2xs">
                      <div class="flex items-center gap-2">

      title: "Amari Dhaka",
      category: "Luxury Hotel",
      location: "Gulshan 2, Dhaka",
      area: "gulshan-2",
      price: "৳ 16,000 / night",
      rating: 4.6,
      reviews: "982                        ${logoHtml}
                        <span class="bg-emerald-100 text-emerald-800 text-[10px]",
      description: "Modern rooms, rooftop pool and easy access to embassies and corporate offices in Gulshan Circle  font-bold px-2 py-0.5 rounded-full">Available</span>
                      </div>

                      <div2.",
      image: "https://images.unsplash.com/photo-1542314831-068cd1dbfeeb?auto=format&fit=crop&w=8 class="flex items-center gap-3 ml-auto">
                        <div class="text-right">
00&q=80",
    },
    {
      slug: "renaissance-dhaka                          <span class="font-serif font-black text-slate-900 text-sm sm:text",
      title: "Renaissance Dhaka Gulshan Hotel",
      category: "Luxury Hotel",
      location: "Gulshan 1, Dhaka",
      area: "gulshan-1",
      price: "৳ -base">${priceDisplay}</span>
                          ${basePriceNum ? `<span class="text-[9px] text22,000 / night",
      rating: 4.7,
      reviews: "1-slate-400 font-bold block -mt-1">+taxes</span>` : ''}
                        </div>,102",
      description: "Stylish hotel with spacious rooms, multiple dining options and warm hospitality in Gulshan 1.",
      image: "https://images.unsplash.com/photo-15827
                        <a href="${supp.link}" target="_blank" rel="nofollow sponsored" class="${buttonClass}">19478250-c89cae4dc85b?auto=format&
                          View deal
                        </a>
                      </div>
                    </div>
                  `;
                  otaContainer.insertAdjacentHTML('fit=crop&w=800&q=80",
    }
  ];
}
---

<Layout title="Gulshan Hotels - Official Accommodation Directory in Gulshan, Dhaka">
  
  <!-- HERO SECTION -->
  <section class="relative bg-[#070D12] text-white py-20 sm:pybeforeend', cardHTML);
                });

              } else {
                renderModalSoldOut(hotelName);
              }

-32 overflow-hidden min-h-[380px] sm:min-h-            } else {
              renderModalSoldOut(hotelName);
            }
          }
        } catch (err) {
          [460px] flex items-center">
    <div class="absolute inset-0 opacity-45 bg-cover bg-center" style="background-image: url('/hero-gulshan.jpg.webp');"></div>
renderModalSoldOut(hotelName);
        }
      }

      function renderModalSoldOut(hotelName    <div class="absolute inset-0 bg-gradient-to-r from-[#070D12] via-[#070D12]/75 to-transparent"></div>

    <div class="relative) {
        if (otaContainer) {
          otaContainer.innerHTML = `
            <div class="p-4 text- max-w-[1550px] mx-auto px-4 sm:px-8 lg:px-12 w-full text-center sm:text-left space-y-5">
      <divcenter space-y-1 bg-rose-50 rounded-2xl border border-rose-200">
              <div class="flex justify-center sm:justify-start">
        <span class="bg-[#D4AF37]/15 text-[#E8D8B0] text-xs font-black uppercase tracking-widest px-4 py-1.5 rounded-full border border-[#D4AF37]/40 shadow- class="text-rose-700 font-bold text-xs">🔴 Rates Updating for ${hotelName}</div>
              <md">
          👑 Dhaka's Premier Hotel Guide
        </span>
      </div>

      <h1 class="text-3xl sm:text-6xl font-black tracking-tight leading-tight text-white max-w-4xl">
        Discover Gulshan's Finest <span class="text-p class="text-[11px] text-slate-600">Please click Full Details to check live dates[#D4AF37]">Luxury Hotels</span>
      </h1>
      
      <p class="text-sm sm:text-lg text-slate-300 max-w-2xl leading-relaxed">
        Compare, book and experience the best hotels in the heart of Dhaka.
      </p>
    </div>
  </section>

  <!-- or contact via WhatsApp below.</p>
            </div>
          `;
        }
      }

      openBtns.forEach(btn => FLOATING AREA FILTER BAR -->
  <section class="bg-[#F8FAF9] relative z-20 -mt-6 sm:-mt-8">
    <div class="max-w- {
        btn.addEventListener('click', () => {
          const title = btn.getAttribute('data-title[1550px] mx-auto px-4 sm:px-8 lg:px-12">
      <div class="bg-white p-3 sm:p-4 rounded-2xl shadow-xl border border-slate') || 'The Westin Dhaka';
          const image = btn.getAttribute('data-image') || '';
          const rating =-200/90 flex flex-wrap items-center justify-center lg:justify-between gap-2.5 sm:gap- btn.getAttribute('data-rating') || '4.8';

          if (modalTitle) modalTitle.4">
        
        <div class="flex flex-wrap items-center justify-center gap-2.5 sm:gap-3">
          <button data-filter="all" class="filter-pill active-innerText = title;
          if (modalImg) modalImg.src = image;
          if (modalRatingpill bg-[#07120E] text-white hover:bg-emerald-950 px-) modalRating.innerText = `★ ${rating} Excellent`;

          modal?.classList.remove('hidden');
5 py-3 rounded-full text-xs sm:text-sm font-black flex items-center space-x-2 shadow-md transition">
            <svg class="w-4 h-4 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><          modal?.classList.add('flex');

          // Trigger Real-Time Worker API Call for this hotel
          fetchRealtimeRatesForpath stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2Modal(title);
        });
      });

      closeBtn?.addEventListener('click', () => {
         0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-modal?.classList.add('hidden');
        modal?.classList.remove('flex');
      });

      modal1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 ?.addEventListener('click', (e) => {
        if (e.target === modal) {
          modal011 v5m-4 0h4"></path></svg>
            <span>All Gulshan</span>
          </button.classList.add('hidden');
          modal.classList.remove('flex');
        }
      });
>

          <button data-filter="gulshan-1" class="filter-pill bg-white hover:bg-slate-100 text-slate-800 border border-slate-300 px-    });
  </script>

</Layout>
