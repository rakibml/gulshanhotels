---
// src/pages/index.astro
import Layout from '../layouts/Layout.ারফিক সার্চ অ্যান্ড জোন ফিল্টার কন্সোল (Interactive Console):**
   - হিরো সেকশনের ভেতরেই একটি প্রastro';
import { getCollection } from 'astro:content';

// ১. স্পন্সরড ডাটা লোড
let sponsorDataিমিয়াম থ্রি-ইন-ওয়ান গ্লাস বার (`backdrop-blur-2xl`):
     -  = null;
try {
  const rawSponsor = import.meta.glob('../content/sponsored/*.md📍 **Zone Selector Tabs** (All Gulshan, Gulshan 1, Gulshan 2, Diplomatic Zone)', { eager: true });
  const list = Object.values(rawSponsor);
  if (list && list.length > 0) {
    sponsorData = (list[0] as any).frontmatter
     - 🔍 **Instant Search Input**
     - ⚡ **"Find Best Rates ➔"** গ্লোয়;
  }
} catch (e) {
  sponsorData = null;
}

if (!sponsorিং অ্যাকশন বাটন।
4. **৪টি লাক্সারি ট্রাস্ট ব্যাজ (Authority Pillars):**
   - Data) {
  sponsorData = {
    enable: true,
    badge: "⭐ Editor's Choice",
    title: "The Westin Dhaka",
    category: "5-Star Luxury • Gulshan🏨 **25+ Verified Hotels**
   - 🛡️ **Diplomatic Security Zone**
   - ⚡ **Real-time Price Matching**
   - ⭐ **4.8/5 Verified Reviews**
5. **১০০% রেসপন্সিভ ( 2",
    rating: 4.8,
    image: "https://images.unsplash.com/photo-1566073771259-6a8506099945?auto=format&fit=crop&w=800&q=80Mobile, Tab, Laptop, Desktop Perfect):**
   - যেকোনো স্ক্রিন সাইজের ডিভাইসে এটি নিখুঁতভাবে অ্যাড",
    buttonText: "Check Best Deal ➔",
    buttonLink: "/the-westin-dhaka"
  };
}

function toTitleCase(str: string) {
  if (!strজাস্ট হয়ে যাবে।

---

### 💻 **সম্পূর্ণ নতুন মাস্টারপিস `src/pages/index) return 'Gulshan Hotel';
  return str.replace(/\w\S*/g, (txt) => txt.charAt(0).toUpperCase.astro` কোড:**

```astro
---
// src/pages/index.astro
import Layout from() + txt.substr(1).toLowerCase());
}

// ২. গুলশানের সব ভেরিফাইড '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// ১. স্প হোটেল লোড করা
let hotels: any[] = [];
try {
  const collectionData = await getCollectionন্সরড ডাটা লোড
let sponsorData = null;
try {
  const rawSponsor = import.meta('hotels');
  if (collectionData && collectionData.length > 0) {
    hotels = collection.glob('../content/sponsored/*.md', { eager: true });
  const list = Object.values(rawData.map((item: any) => ({
      slug: item.id || item.slug,
      title: toTitleCase(Sponsor);
  if (list && list.length > 0) {
    sponsorData = (listitem.data?.title || item.data?.name || 'Gulshan Hotel'),
      category: item.data[0] as any).frontmatter;
  }
} catch (e) {
  sponsorData =?.category || 'Luxury Hotel',
      location: item.data?.location || 'Gulshan 2, Dhaka null;
}

if (!sponsorData) {
  sponsorData = {
    enable: true,
',
      zone: (item.data?.location || '').toLowerCase().includes('gulshan 1') 
    badge: "⭐ Editor's Choice",
    title: "The Westin Dhaka",
    category:            ? 'gulshan-1' 
            : (item.data?.location || '').toLowerCase().includes(' "5-Star Luxury • Gulshan 2",
    rating: 4.8,
    image:banani')
            ? 'banani'
            : 'gulshan-2',
      price: item "https://images.unsplash.com/photo-1566073771259.data?.price || '$$$$',
      rating: item.data?.rating || 4.8,-6a8506099945?auto=format&fit=crop&w=800&q=80",
    buttonText: "Check Best Deal ➔",
    buttonLink: "/the-westin
      image: item.data?.image || 'https://images.unsplash.com/photo-1566073771259-6a8506099945?-dhaka"
  };
}

function toTitleCase(str: string) {
  if (!auto=format&fit=crop&w=800&q=80',
    }));
  str) return 'Gulshan Hotel';
  return str.replace(/\w\S*/g, (txt)}
} catch (err) {
  hotels = [];
}
---

<Layout title="Gulshan => txt.charAt(0).toUpperCase() + txt.substr(1).toLowerCase());
}

// ২. গুলশানের সব ভেরিফাইড হোটেল লোড করা
let hotels: any[] = [];
try {
 Hotels - Official Accommodations & Luxury Travel Directory in Gulshan, Dhaka">
  
  <!-- ULTIMATE LUXURY HERO BANNER SECTION -->
  <section class="relative bg-slate-950 text-white min-h-  const collectionData = await getCollection('hotels');
  if (collectionData && collectionData.length > 0) {
    hotels = collectionData.map((item: any) => ({
      slug: item.id || item.slug,
      title: toTitleCase(item.data?.title || item.data?.[85vh] lg:min-h-[90vh] flex items-center justify-center overflow-hiddenname || 'Gulshan Hotel'),
      category: item.data?.category || 'Luxury Hotel',
      location: item.data?.location || border-b border-emerald-500/20">
    
    <!-- Background High-Quality Evening 'Gulshan 2, Dhaka',
      zone: (item.data?.location || '').toLowerCase().includes(' Skyline Image & Ambient Video Overlay -->
    <div class="absolute inset-0 w-full h-full">
      <imggulshan 1') 
            ? 'gulshan-1' 
            : (item.data?. 
        src="https://images.unsplash.com/photo-151291777location || '').toLowerCase().includes('banani')
            ? 'banani'
            : 'gulshan-4080-9991f1c4c750?auto=format&fit=2',
      price: item.data?.price || '$$$$',
      rating: item.data?.rating || 4.8,
      image: item.data?.image || 'https://images.unsplash.crop&w=2000&q=85" 
        alt="Gulshan Dhaka Diplomatic District Evening View" 
        class="w-full h-full object-cover opacity-40 scale-105 transformcom/photo-1566073771259-6a8506 transition duration-1000" 
      />
      <!-- Soft Radial Light & Deep Gradient Overlay -->
099945?auto=format&fit=crop&w=800&q=80',
    }));
  }
} catch (err) {
  hotels = [];
}
---

      <div class="absolute inset-0 bg-gradient-to-t from-slate-950 via<Layout title="Gulshan Hotels - Official Directory & Luxury Accommodations Guide">
  
  <!-- MASTERPIECE-slate-950/70 to-slate-950/50"></div>
      <div class="absolute inset-0 HERO SECTION -->
  <section class="relative bg-slate-950 text-white min-h- bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-emerald-9[85vh] sm:min-h-[90vh] flex flex-col justify-center overflow-hidden border-b border50/30 via-transparent to-transparent"></div>
    </div>

    <!-- MAIN HERO CONTENT CONTAINER -->
    <div class="relative-slate-800/80">
    
    <!-- Background Image with Ambient Emerald & Gold Radial Glow Overlay z-10 max-w-[1280px] mx-auto px-4 sm:px-6 lg:px-8 pt -->
    <div class="absolute inset-0 w-full h-full pointer-events-none">
      <-20 pb-28 text-center space-y-6 sm:space-y-8">
      
      <!-- Premiumimg 
        src="https://images.unsplash.com/photo-15129177 Gold/Emerald Badge -->
      <div class="inline-flex items-center gap-2 bg-gradient-to-74080-9991f1c4c750?auto=format&fit=crop&w=1920&q=80" 
        alt="Gulshan Skyline Eveningr from-emerald-500/20 via-teal-500/20 to-emerald-500/20 border border-emerald-400/40 text-emerald-300 text-xs font View" 
        class="w-full h-full object-cover opacity-25 scale-105 transform-black uppercase tracking-widest px-4 py-2 rounded-full backdrop-blur-md shadow-xl">
        <span transition duration-1000" 
      />
      <!-- Radial Aura Glow -->
      <div class="absolute inset class="w-2 h-2 rounded-full bg-emerald-400 animate-ping"></span>
        Gul-0 bg-gradient-to-t from-slate-950 via-slate-950/shan's Official Accommodations & Travel Authority
      </div>

      <!-- Master Headline -->
      <h1 class="text75 to-slate-950/40"></div>
      <div class="absolute top-1-3xl sm:text-6xl lg:text-7xl font-black tracking-tight text-white leading-tight font-serif/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w- max-w-5xl mx-auto drop-shadow-2xl">
        Discover World-Class Stays In[600px] h-[350px] bg-emerald-500/15 blur-[120px] rounded- <br class="hidden sm:inline" />
        <span class="text-transparent bg-clip-textfull pointer-events-none"></div>
    </div>

    <!-- MAIN HERO CONTENT -->
    <div class="relative z-10 max bg-gradient-to-r from-emerald-400 via-teal-200 to-amber-w-[1280px] mx-auto px-4 sm:px-6 lg:px--300">Gulshan, Dhaka</span>
      </h1>

      <!-- Subtitle -->
      <p class8 pt-24 pb-16 text-center space-y-6 sm:space-y-8">
      
      <!-- Official Badge -->
      <div class="inline-flex items-center gap-2 bg-gradient-to-r="text-sm sm:text-xl text-slate-200 max-w-3xl mx-auto font-normal leading-relaxed drop-shadow">
        Your unbiased local authority for 5-star luxury suites, diplomatic lodging, corporate accommodations from-emerald-500/20 via-slate-900/80 to-amber-50, and real-time deal comparison across Gulshan 1, Gulshan 2 & Baridhara Diplomatic Enclosure.
      </p0/20 border border-emerald-500/40 text-emerald-300 text->

      <!-- Action Buttons -->
      <div class="pt-2 flex flex-wrap justify-center items-center gap-3 sm:gap-4">
        <a 
          href="#all-hotels"xs sm:text-sm font-black uppercase tracking-widest px-4 py-2 rounded-full backdrop-blur-md 
          class="bg-emerald-600 hover:bg-emerald-500 text-white shadow-xl">
        <span class="w-2.5 h-2.5 rounded-full bg-emerald- font-black text-xs sm:text-base px-8 py-4 rounded-full shadow-2xl400 animate-ping"></span>
        <span>GULSHAN'S OFFICIAL LODGING DIRECTORY</span>
      </div>

 shadow-emerald-950/60 transition hover:scale-105 border border-emerald-400/      <!-- Jaw-Dropping Masterpiece Headline -->
      <h1 class="text-3xl xs:text-430 flex items-center gap-2"
        >
          <span>Explore Verified Hotels</span>
          <spanxl sm:text-6xl lg:text-7xl font-black tracking-tight text-white leading-tight font-serif select class="text-lg">➔</span>
        </a>

        <a 
          href="#diplomatic-zone" 
          class-none drop-shadow-2xl">
        Discover Luxury & Comfort In <br class="hidden sm:inline" />
        <span class="bg-white/10 hover:bg-white/20 border border-white/20 text-white font-extrabold="text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 via text-xs sm:text-base px-7 py-4 rounded-full transition backdrop-blur-md hover-teal-200 to-amber-300">
          Gulshan, Dhaka
        </span>
      </h1>

      :scale-105 flex items-center gap-2"
        >
          <span>🏛️ Diplomatic Zone Stays</span>
        </a>
      </div>

    </div>

    <!-- Soft Curved Wave Divider -->
    <div class<p class="text-xs sm:text-base lg:text-lg text-slate-300 max-w-2xl mx-auto font-normal leading-relaxed drop-shadow">
        Your verified local guide to 5-star luxury stays, diplomatic="absolute bottom-0 left-0 right-0 w-full overflow-hidden leading-none z-10 lodging, executive business suites, and real-time deal comparison.
      </p>

      <!-- ALL-IN-ONE GLASSMORPHIC SEARCH & AREA CONSOLE -->
      <div class="max-w-4xl mx-auto pt-4"> pointer-events-none">
      <svg class="relative block w-full h-8 sm:h-14 text-slate-50" viewBox="0 0 1200 120" preserveAspectRatio="none" fill="currentColor">
        <path d="M0,0 C150,90 350,-
        <div class="bg-slate-900/75 border border-slate-70040 500,60 C650,160 900,10 1200,/60 rounded-3xl p-3 sm:p-4 backdrop-blur-2xl shadow-2xl space40 L1200,120 L0,120 Z"></path>
      </-y-3">
          
          <!-- Zone Filter Tabs -->
          <div class="flex flex-wrap items-center justifysvg>
    </div>

  </section>

  <!-- FLOATING INTERACTIVE DIRECTORY CONSOLE -->
  <div class="relative z-20 -mt-8 sm:-mt-10 max-w-[1280px] mx-auto px--center gap-1.5 sm:gap-2 border-b border-slate-800 pb-3">
            <4 sm:px-6 lg:px-8">
    <div class="bg-white rounded-2xl sm:rounded-full border border-slate-200 shadow-2xl p-2.5 smbutton data-filter="all" class="filter-tab active bg-emerald-600 text-white font-extrabold text:p-3 flex items-center justify-between gap-2 overflow-x-auto [scrollbar-width:none]-xs px-4 py-2 rounded-full transition shadow">
              🏢 All Gulshan ({hotels.length [-ms-overflow-style:none] [&::-webkit-scrollbar]:hidden">
      
      <div class="flex items-})
            </button>
            <button data-filter="gulshan-1" class="filter-tabcenter gap-2 shrink-0 w-full sm:w-auto overflow-x-auto">
        < bg-slate-800/80 hover:bg-slate-700 text-slate-2button 
          data-filter="all" 
          class="filter-pill active bg-00 font-bold text-xs px-4 py-2 rounded-full transition">
              📍 Gulshan 1
            </button>
            <button data-filter="gulshan-2" class="filter-[#052e16] text-white font-black text-xs sm:text-sm px-5 py-3tab bg-slate-800/80 hover:bg-slate-700 text-slate- rounded-full transition flex items-center gap-2 shrink-0 shadow-lg"
        >
          <svg class="w-4 h-4 fill-current" viewBox="0 0 24 24"><path d="M19 2H9c-1.1 0-2 .9-2200 font-bold text-xs px-4 py-2 rounded-full transition">
              📍 Gulshan 2
            </button>
            <button data-filter="banani" class="filter-tab 2v16c0 1.1.9 2 2 2h10c1 bg-slate-800/80 hover:bg-slate-700 text-slate-200 font-bold text-xs px-4 py-2 rounded-full transition">
              🏛️ Dipl.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm0 18H9V4h10v16z"/></svg>
omatic Zone
            </button>
          </div>

          <!-- Instant Search Input & CTA Button -->
          <div          <span>All Gulshan Hotels ({hotels.length})</span>
        </button>

        <button 
           class="flex flex-col sm:flex-row items-center gap-2">
            <div class="data-filter="gulshan-1" 
          class="filter-pill bg-white border border-slaterelative w-full">
              <input 
                type="text" 
                id="searchHotels" 
                placeholder="Search hotel by name (e.g. Westin, Amari, Renaissance)..." 
                class="w-full bg-200 hover:bg-slate-50 text-slate-800 font-extrabold text-xs sm:text-sm px-5 py-3 rounded-full transition flex items-center gap-2 shrink-0"
        >
          <svg class="w-4 h-4 fill-emerald--slate-950/80 border border-slate-700 focus:border-emerald-500 rounded-2600" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.2xl px-4 py-3 text-xs sm:text-sm text-white placeholder-slate-400 outline-none transition"5 7 13 7 13s7-7.75 7-13c
              />
              <span class="absolute right-3.5 top-3.5 text-slate-40-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
          <span>Gulshan 1</span>
        </button>

        <button 
          data-filter="00 text-sm">🔍</span>
            </div>

            <a href="#all-hotels" class="w-full sm:w-auto bg-emerald-600 hover:bg-emerald-500 text-white font-black text-xs sm:text-sm px-7 py-3 rounded-2xl transition shrink-0 shadow-lg flexgulshan-2" 
          class="filter-pill bg-white border border-slate-200 items-center justify-center gap-2">
              <span>Find Rates</span>
              <span>➔</span>
            </a>
          </div>

        </div>
      </div>

    </div>

    <!-- TRUST & AUTHORITY PILLARS BAR -->
    <div class="relative hover:bg-slate-50 text-slate-800 font-extrabold text-xs sm:text-sm px-5 py-3 rounded-full transition flex items-center gap-2 shrink-0"
        >
          < z-10 max-w-[1280px] mx-auto px-4 pb-12">
      <div class="svg class="w-4 h-4 fill-emerald-600" viewBox="0 0 2grid grid-cols-2 md:grid-cols-4 gap-3 sm:gap-4">
        4 24"><path d="M12 2C8.13 2 5 5
        <div class="bg-slate-900/60 border border-slate-800.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13/80 p-3.5 rounded-2xl backdrop-blur-md flex items-center gap-3">
          <span class-7-7-7zm0 9.5c-1.38 0-2.5="text-2xl p-2 bg-emerald-950/80 text-emerald-400 rounded-xl border border--1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.emerald-500/20">🛡️</span>
          <div>
            <h3 class="text-xs font-black text-white uppercase">100% Verified</h3>
            <p class="text-[10px] text-slate-5-2.5 2.5z"/></svg>
          <span>Gulshan 2</span>
        </button>

        <button 
          data-filter="banani" 
          class="filter-pill bg-white border border-400">Inspected Physical Stays</p>
          </div>
        </div>

        <div class="bg-slate-900/60 border border-slate-800/80 p-3.5 rounded-2xl backdropslate-200 hover:bg-slate-50 text-slate-800 font-extrabold text-xs sm:text-sm px-5 py-3 rounded-full transition flex items-center gap-blur-md flex items-center gap-3">
          <span class="text-2xl p-2 bg-emerald-950/80 text-emerald-400 rounded-xl border border--2 shrink-0"
        >
          <svg class="w-4 h-4 fill-emeraldemerald-500/20">⚡</span>
          <div>
            <h3 class="text-xs-600" viewBox="0 0 24 24"><path d="M12  font-black text-white uppercase">Live Price Match</h3>
            <p class="text-[10px]7V3H2v18h20V7H12z"/></svg>
          <span>Diplomatic Zone</span>
        </button>
      </div>

      <button 
        id="toggleMoreFilters" 
        class="bg text-slate-400">Agoda, Booking & Direct</p>
          </div>
        </div>

-emerald-600 hover:bg-emerald-500 text-white font-black text-xs sm:text-sm        <div class="bg-slate-900/60 border border-slate-800/ px-5 py-3 rounded-full transition flex items-center gap-2 shrink-0 ml-auto shadow80 p-3.5 rounded-2xl backdrop-blur-md flex items-center gap-3">-lg"
      >
        <span>🔍 Search Directory</span>
      </button>

    </div>

    <!-- EXPANDABLE SEARCH
          <span class="text-2xl p-2 bg-emerald-950/80 text PANEL -->
    <div id="moreFiltersPanel" class="hidden mt-3 bg-white p-5-emerald-400 rounded-xl border border-emerald-500/20">🏛️</span> sm:p-6 rounded-2xl border border-slate-200 shadow-2xl">
      <div class="grid grid
          <div>
            <h3 class="text-xs font-black text-white uppercase">Diplomatic Zone-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label class="</h3>
            <p class="text-[10px] text-slate-400">Near Foreign Embassies</p>
          </div>
        </div>

        <div class="bg-slate-900/60 border border-slate-800/80 p-3.5 rounded-2xl backdrop-blur-md flex items-centerblock text-xs font-black text-slate-700 uppercase mb-1">Search Hotel Name</label>
          <input type="text" id="searchHotels" placeholder="e.g. Westin, gap-3">
          <span class="text-2xl p-2 bg-emerald-950 Amari, Renaissance..." class="w-full bg-slate-50 border border-slate-300 focus:border-emerald-600 focus:bg-white rounded-xl px-4 py-2./80 text-emerald-400 rounded-xl border border-emerald-500/205 text-xs text-slate-900 outline-none" />
        </div>
        <div class">⭐</span>
          <div>
            <h3 class="text-xs font-black text-white uppercase">4.8/5 Rating</h3>
            <p class="text-[10px] text-slate-4="flex items-end">
          <button id="resetFilters" class="w-full bg-slate-00">Verified Local Reviews</p>
          </div>
        </div>

      </div>
    </div>

  </section>

  <!-- MAIN HOTEL DIRECTORY GRID -->
  <section id="all-hotels" class="py100 hover:bg-slate-200 text-slate-800 font-extrabold text-xs py-2.5 rounded-xl transition">Clear Search</button>
        </div>
      </div>
    </div>
  </div>

  <!-- TRUST & AUTHORITY BADGES (4 KEY PILLARS) -->
  <section class="pt-14 pb-10 px-4 sm:px-6 lg:px-8 max-w-[1280px] mx-auto">
    <-12 px-4 sm:px-6 lg:px-8 max-w-[1280px] mx-div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
      
      auto text-slate-800">
    
    <div class="flex justify-between items-end mb-8 border-b border-slate-200 pb-4">
      <div>
        <span class="text-xs font<div class="bg-white p-4 sm:p-5 rounded-2xl border border-slate-200-black uppercase text-emerald-800 tracking-wider">Official Accommodations</span>
        <h2 class shadow-sm flex items-center gap-3">
        <span class="text-2xl sm:text-3xl p-2="text-2xl sm:text-4xl font-black text-slate-900 font-serif">
          All.5 bg-emerald-50 text-emerald-700 rounded-xl">🛡️</span>
        <div>
          <h Gulshan Hotels ({hotels.length})
        </h2>
      </div>
    </div>

    <!-- Hotel Cards Grid -->3 class="text-xs sm:text-sm font-black text-slate-900 uppercase">100% Verified</h3>
          
    <div id="hotelsGrid" class="grid grid-cols-1 sm:grid-cols-2<p class="text-[11px] sm:text-xs text-slate-500">Physically inspected stays</p>
        </div>
      </div>

      <div class="bg-white p-4 sm:p lg:grid-cols-3 gap-6">
      {hotels.map((hotel) => (
        <div 
          class="hotel-card bg-white rounded-2xl border border-slate-200 shadow-5 rounded-2xl border border-slate-200 shadow-sm flex items-center gap-3-md hover:shadow-2xl hover:-translate-y-1 transition duration-300 overflow-hidden flex flex-col group"
          data-zone={hotel.zone}
          data-title={hotel.title.toLowerCase()}
        ">
        <span class="text-2xl sm:text-3xl p-2.5 bg->
          <div class="h-48 sm:h-56 w-full overflow-hidden relative">
            <img 
              src={hotel.image} 
              alt={hotel.title} emerald-50 text-emerald-700 rounded-xl">⚡</span>
        <div>
          <h3 class="text-xs sm:text-sm font-black text-slate-900 uppercase">Live
              loading="lazy"
              decoding="async"
              class="w-full h-full object Price Match</h3>
          <p class="text-[11px] sm:text-xs text-slate-500">Agoda, Booking & Direct</p>
        </div>
      </div>

      <div class-cover group-hover:scale-105 transition duration-500" 
            />
            
            <div class="absolute top-3 left-3 bg-slate-950/90 text="bg-white p-4 sm:p-5 rounded-2xl border border-slate-200-emerald-300 text-[10px] font-black uppercase px-2.5 py-1 shadow-sm flex items-center gap-3">
        <span class="text-2xl sm:text rounded-md border border-emerald-500/40 backdrop-blur-md shadow">
              ✔ Verified Entry
            </div>

            <div class="absolute top-3 right-3 bg-slate-90-3xl p-2.5 bg-emerald-50 text-emerald-700 rounded-xl0/85 backdrop-blur-md text-amber-400 text-xs font-black px-">🏛️</span>
        <div>
          <h3 class="text-xs sm:text-sm font-2.5 py-1 rounded-md border border-slate-700 shadow-md">
              ★black text-slate-900 uppercase">Diplomatic Zone</h3>
          <p class="text-[11px] sm:text-xs text-slate-500">Near US & Foreign Embassies</p>
        </div>
      </div>

      <div class="bg-white p-4 sm:p-5 rounded-2xl border border-slate-200 shadow-sm flex items-center gap-3">
         {hotel.rating}
            </div>

            <div class="absolute bottom-3 left-3 bg-emerald-950/90 text-emerald-300 text-[10px] font-black uppercase<span class="text-2xl sm:text-3xl p-2.5 bg-emerald-5 px-2.5 py-1 rounded-md border border-emerald-500/40 backdrop-blur-md">
              {hotel.category}
            </div>
          </div>

          <div class="p0 text-emerald-700 rounded-xl">📞</span>
        <div>
          <h3 class="-5 flex flex-col flex-1 justify-between">
            <div>
              <h3 class="font-seriftext-xs sm:text-sm font-black text-slate-900 uppercase">Direct Booking</h3>
 text-lg sm:text-xl font-bold text-slate-900 mb-2 group-hover:text-          <p class="text-[11px] sm:text-xs text-slate-500">WhatsApp & Direct Calls</p>
        </div>
      </div>

    </div>
  </section>

  <!--emerald-700 transition">
                {hotel.title}
              </h3>

              <div class="flex items-center gap-1.5 text-xs text-slate-500 mb-6">
                <svg class="w-4 h-4 fill-emerald-600 shrink-0" viewBox="0 SPONSORED SHOWCASE CARD -->
  {sponsorData && sponsorData.enable ? (
    <section class="pb-12 px-4 sm:px-6 lg:px-8 max-w- 0 24 24">
                  <path d="M12 2C8.1[1280px] mx-auto">
      <div class="relative rounded-3xl overflow-hidden shadow-2xl3 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3. border border-slate-200 bg-slate-900 text-white grid grid-cols-1 lg:grid-cols-12 items-center">
        
        <div class="lg:col-87-3.13-7-7-7zm0 9.5c-1.3span-7 h-64 sm:h-80 relative overflow-hidden">
          <img src={8 0-2.5-1.12-2.5-2.5s1.1sponsorData.image} alt={sponsorData.title} class="w-full h-full object-cover group-hover:scale-2-2.5 2.5-2.5 2.5 1.12 2.5 2.5105 transition duration-500 opacity-90" />
          <div class="absolute inset-0 bg-gradient-to-r from-slate-950/80 via-transparent to-transparent"></div>
          
          -1.12 2.5-2.5 2.5z"/>
                </svg>
                <span class="<div class="absolute top-4 left-4 bg-amber-500 text-slate-95line-clamp-1">{hotel.location}</span>
              </div>
            </div>

            <div class="pt0 text-xs font-black uppercase px-3.5 py-1 rounded-full shadow-lg">
-4 border-t border-slate-100 flex items-center justify-between">
              <div>
                <span class="text-[10px] uppercase text-slate-400 font-extrabold            {sponsorData.badge || "⭐ Featured Choice"}
          </div>
        </div>

        <div class=" block">Price Range</span>
                <span class="font-black text-slate-900 text-smlg:col-span-5 p-6 sm:p-8 space-y-4">
          <span class="text- sm:text-base">{hotel.price}</span>
              </div>

              <a href={`/${hotel.slugxs font-black uppercase text-emerald-400 bg-emerald-950 px-3 py-}`} class="bg-emerald-600 hover:bg-emerald-500 text-white font-1 rounded-md border border-emerald-500/30">
            {sponsorData.category}extrabold text-xs px-4 py-2.5 rounded-xl shadow-md transition flex items-center gap-1">
                
          </span>
          
          <h2 class="text-2xl sm:text-3xl font-Compare Rates ➔
              </a>
            </div>
          </div>
        </div>
      ))}
    </div>

  black text-white">{sponsorData.title}</h2>
          
          <p class="text-xs sm:text-sm text-slate-300 leading-relaxed">
            Gulshan's premier luxury stay with world-class executive amenities, heated pool, diplomatic security,</section>

  <!-- INSTANT SEARCH & FILTER SCRIPT -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const tabs = document.querySelectorAll('.filter-tab');
      const cards = document and fine dining restaurants.
          </p>

          <div class="pt-2 flex items-center justify.querySelectorAll('.hotel-card');
      const searchInput = document.getElementById('searchHotels') as HTMLInputElement-between">
            <span class="text-amber-400 font-bold text-xs sm:text-sm">★ {sponsor;

      let currentZone = 'all';

      function applyFilter() {
        const query = searchInputData.rating} / 5 Excellent</span>
            
            <a 
              href={sponsorData.button ? searchInput.value.toLowerCase().trim() : '';

        cards.forEach((card) => {
          const el = card as HTMLElement;
          const zone = el.getAttribute('data-zone');
          const title = el.getAttribute('data-title') || '';

          const matchesZone = currentZone === 'all' ||Link || "/the-westin-dhaka"} 
              class="bg-emerald-600 hover zone === currentZone;
          const matchesSearch = query === '' || title.includes(query);

          if:bg-emerald-500 text-white font-black text-xs sm:text-sm px-5 py (matchesZone && matchesSearch) {
            el.style.display = 'flex';
          } else {-3 rounded-xl shadow-lg transition"
            >
              {sponsorData.buttonText || "Check
            el.style.display = 'none';
          }
        });
      }

      tabs. Best Price ➔"}
            </a>
          </div>
        </div>

      </div>
    </section>
  forEach((tab) => {
        tab.addEventListener('click', () => {
          tabs.forEach((t) : null}

  <!-- MAIN HOTEL DIRECTORY GRID -->
  <section id="all-hotels" class="py-10 px-4 sm:px-6 lg:px-8 max-w-[1280px] mx-auto">
    
    <div class="flex justify-between items-end mb) => {
            t.classList.remove('bg-emerald-600', 'text-white',-8 border-b border-slate-200 pb-4">
      <div>
        <span class 'shadow');
            t.classList.add('bg-slate-800/80', 'text-slate-200');
          });

          tab.classList.remove('bg-slate-800/80', 'text="text-xs font-black uppercase text-emerald-800 tracking-wider">Verified Directory Entries</span>
        <h2 class-slate-200');
          tab.classList.add('bg-emerald-600', '="text-3xl sm:text-4xl font-black text-slate-900 font-seriftext-white', 'shadow');

          currentZone = tab.getAttribute('data-filter') || 'all';">
          All Gulshan Hotels ({hotels.length})
        </h2>
      </div>
    </div>

    <!-- Hotel Cards Grid -->
    <div id="hotelsGrid" class="grid grid-cols-1 sm:grid
          applyFilter();
        });
      });

      if (searchInput) searchInput.addEventListener('input',-cols-2 lg:grid-cols-3 gap-6">
      {hotels.map((hotel) applyFilter);
    });
  </script>

</Layout>
``` => (
        <div 
          class="hotel-card bg-white rounded-2xl border border-
