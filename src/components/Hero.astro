---
// src/components/Hero.astro (Cinematic Video Background + 50% Overlapping Bottom Search Bar)
---

<!-- CINEMATIC HERO SECTION -->
<section class="relative bg-slate-950 min-h-[460px] sm:min-h-[520px] lg:min-h-[560px] flex flex-col justify-between text-left">
  
  <!-- ================= 1. CINEMATIC BACKGROUND VIDEO ================= -->
  <div class="absolute inset-0 w-full h-full overflow-hidden pointer-events-none">
    <video 
      id="heroBgVideo"
      autoplay 
      loop 
      muted 
      playsinline 
      preload="auto"
      poster="/hero-gulshan.jpg.webp"
      class="w-full h-full object-cover object-center scale-105 transition-opacity duration-1000"
    >
      <!-- আপনার লোকাল ভিডিও (public/hero-video.mp4) -->
      <source src="/hero-video.mp4.mp4" type="video/mp4" />
      <!-- ওপেন ক্লাউড সিডিএন ব্যাকআপ ভিডিও -->
      <source src="https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerBlazes.mp4" type="video/mp4" />
    </video>

    <!-- Deep Cinematic Overlay for High-Contrast Text Readability -->
    <div class="absolute inset-0 bg-gradient-to-t from-slate-950 via-slate-950/65 to-slate-900/50"></div>
  </div>

  <!-- ================= 2. H1 HEADLINE (CENTERED IN HERO AREA) ================= -->
  <div class="relative z-10 max-w-[1240px] mx-auto w-full px-4 sm:px-6 lg:px-8 pt-24 sm:pt-32 pb-12 text-center my-auto">
    <h1 class="font-serif text-3xl sm:text-5xl lg:text-6xl font-black text-white tracking-tight leading-[1.14] max-w-4xl mx-auto drop-shadow-2xl">
      Hotels & Luxury Suites in Gulshan, Dhaka
    </h1>
  </div>

  <!-- ================= 3. OVERLAPPING SEARCH CONSOLE (HALF ON VIDEO, HALF ON WHITE) ================= -->
  <div class="relative z-30 max-w-4xl mx-auto w-full px-4 sm:px-6 lg:px-8 translate-y-1/2">
    <div class="bg-white rounded-2xl shadow-2xl p-2.5 sm:p-3 border border-slate-200/90 text-left">
      <form action="/hotels" method="GET" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-12 gap-2 items-center">
        
        <!-- Zone / Location -->
        <div class="lg:col-span-4 p-2 sm:px-3 bg-slate-50/90 rounded-xl border border-slate-200 hover:border-slate-300 transition">
          <label class="text-[9px] font-black uppercase text-slate-400 tracking-wider block leading-none mb-1">
            Zone / Location
          </label>
          <div class="flex items-center gap-2">
            <svg class="w-3.5 h-3.5 text-[#006ce4] shrink-0" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/>
            </svg>
            <select name="area" class="w-full text-xs font-bold text-slate-800 bg-transparent outline-none cursor-pointer">
              <option value="all">All Gulshan District</option>
              <option value="gulshan-1">Gulshan 1 (Commercial Hub)</option>
              <option value="gulshan-2">Gulshan 2 (Diplomatic Enclave)</option>
              <option value="near">Near Gulshan / Banani</option>
            </select>
          </div>
        </div>

        <!-- Property Category -->
        <div class="lg:col-span-4 p-2 sm:px-3 bg-slate-50/90 rounded-xl border border-slate-200 hover:border-slate-300 transition">
          <label class="text-[9px] font-black uppercase text-slate-400 tracking-wider block leading-none mb-1">
            Property Category
          </label>
          <div class="flex items-center gap-2">
            <svg class="w-3.5 h-3.5 text-emerald-700 shrink-0" viewBox="0 0 24 24" fill="currentColor">
              <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-7 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm3 10H9v-1c0-1 2-1.9 3-1.9s3 .9 3 1.9v1z"/>
            </svg>
            <select name="type" class="w-full text-xs font-bold text-slate-800 bg-transparent outline-none cursor-pointer">
              <option value="all">All Properties</option>
              <option value="hotel">5-Star & Luxury Hotels</option>
              <option value="guesthouse">Boutique Guest Houses</option>
            </select>
          </div>
        </div>

        <!-- Search Button -->
        <div class="sm:col-span-2 lg:col-span-4 flex items-center">
          <button 
            type="submit" 
            class="w-full bg-[#003b95] hover:bg-[#002b6e] text-white font-black text-xs sm:text-sm py-3.5 px-6 rounded-xl shadow-md transition-all flex items-center justify-center gap-2 cursor-pointer active:scale-98 tracking-wide"
          >
            <span>Search Properties</span>
            <svg class="w-4 h-4 fill-current" viewBox="0 0 24 24">
              <path d="M10 6L8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z"/>
            </svg>
          </button>
        </div>

      </form>
    </div>
  </div>

</section>

<!-- ================= 4. AUTOPLAY SCRIPT ================= -->
<script is:inline>
  const playHeroVideo = () => {
    const video = document.getElementById('heroBgVideo');
    if (video) {
      video.muted = true;
      const playPromise = video.play();
      if (playPromise !== undefined) {
        playPromise.catch(() => {
          const triggerPlay = () => {
            video.play();
            window.removeEventListener('scroll', triggerPlay);
            window.removeEventListener('touchstart', triggerPlay);
          };
          window.addEventListener('scroll', triggerPlay, { passive: true });
          window.addEventListener('touchstart', triggerPlay, { passive: true });
        });
      }
    }
  };

  document.addEventListener('DOMContentLoaded', playHeroVideo);
  document.addEventListener('astro:page-load', playHeroVideo);
</script>
