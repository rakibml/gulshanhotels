---
// src/components/directory/StickyBar.astro
---

<header class="sticky top-0 z-[100] bg-white/95 backdrop-blur-md border-b border-slate-200 font-['Plus_Jakarta_Sans',sans-serif]">
  <div class="max-w-[1280px] mx-auto px-4 py-2.5">
    
    <!-- ১. সার্চ বার -->
    <div class="relative group mb-3">
      <div class="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" stroke-width="2.5" stroke-linecap="round"/></svg>
      </div>
      <input 
        type="text" 
        id="hotelSearchInput" 
        placeholder="Search hotel name..." 
        class="w-full bg-slate-100 focus:bg-white text-sm font-bold rounded-full pl-10 pr-4 py-2.5 outline-none border border-transparent focus:border-blue-500 transition-all shadow-inner" 
      />
    </div>

    <!-- ২. পিল বাটন কন্টেইনার -->
    <div class="flex items-center gap-2 overflow-x-auto md:overflow-visible no-scrollbar relative pb-1">
      
      <!-- PRICE PILL -->
      <div class="relative pill-container shrink-0">
        <button type="button" class="pill-trigger shrink-0 flex items-center gap-1.5 px-4 py-2 rounded-full border border-slate-200 bg-white text-[13px] font-bold text-slate-700 transition-all active:scale-95 hover:border-slate-400 cursor-pointer whitespace-nowrap outline-none" data-target="priceBox">
          <svg class="w-3.5 h-3.5 text-emerald-600" fill="currentColor" viewBox="0 0 24 24"><path d="M12.5 3H7.8a2 2 0 0 0-1.4.6l-3.8 3.8a2 2 0 0 0 0 2.8l8.2 8.2a2 2 0 0 0 2.8 0l3.8-3.8c.4-.4.6-.9.6-1.4V8.5a5.5 5.5 0 0 0-5.5-5.5z"/></svg>
          <span>Price</span>
          <svg class="w-3 h-3 text-slate-400 transition-transform arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M19 9l-7 7-7-7" stroke-width="2.5" stroke-linecap="round"/></svg>
        </button>
        <!-- Price Dropdown -->
        <div id="priceBox" class="filter-popup hidden absolute top-[calc(100%+8px)] left-0 w-[240px] bg-white border border-slate-200 rounded-2xl shadow-[0_10px_40px_rgba(0,0,0,0.15)] p-5 z-[1000]">
          <h4 class="text-[11px] font-black uppercase text-slate-400 mb-3 tracking-widest">Price Range</h4>
          <div class="space-y-3">
             <label class="flex items-center justify-between cursor-pointer group">
               <span class="text-sm font-bold text-slate-700 group-hover:text-blue-600">All Prices</span>
               <input type="radio" name="price" value="all" checked class="w-4 h-4 accent-blue-600 sticky-filter-input">
             </label>
             <label class="flex items-center justify-between cursor-pointer group">
               <span class="text-sm font-bold text-slate-700 group-hover:text-blue-600">Under $50</span>
               <input type="radio" name="price" value="low" class="w-4 h-4 accent-blue-600 sticky-filter-input">
             </label>
             <label class="flex items-center justify-between cursor-pointer group">
               <span class="text-sm font-bold text-slate-700 group-hover:text-blue-600">Above $50</span>
               <input type="radio" name="price" value="high" class="w-4 h-4 accent-blue-600 sticky-filter-input">
             </label>
          </div>
        </div>
      </div>

      <!-- RATING PILL -->
      <div class="relative pill-container shrink-0">
        <button type="button" class="pill-trigger shrink-0 flex items-center gap-1.5 px-4 py-2 rounded-full border border-slate-200 bg-white text-[13px] font-bold text-slate-700 transition-all active:scale-95 hover:border-slate-400 cursor-pointer whitespace-nowrap outline-none" data-target="ratingBox">
          <svg class="w-3.5 h-3.5 text-amber-500" fill="currentColor" viewBox="0 0 24 24"><path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"/></svg>
          <span>Rating</span>
          <svg class="w-3.5 h-3.5 text-slate-400 transition-transform arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M19 9l-7 7-7-7" stroke-width="2.5" stroke-linecap="round"/></svg>
        </button>
        <div id="ratingBox" class="filter-popup hidden absolute top-[calc(100%+8px)] left-0 w-[220px] bg-white border border-slate-200 rounded-2xl shadow-[0_10px_40px_rgba(0,0,0,0.15)] p-5 z-[1000]">
          <h4 class="text-[11px] font-black uppercase text-slate-400 mb-3 tracking-widest">Guest Rating</h4>
          <div class="space-y-3">
             <label class="flex items-center gap-2 cursor-pointer"><input type="checkbox" value="9" class="w-4 h-4 accent-blue-600 sticky-filter-input"><span class="text-sm font-bold text-slate-700">Wonderful 9+</span></label>
             <label class="flex items-center gap-2 cursor-pointer"><input type="checkbox" value="8" class="w-4 h-4 accent-blue-600 sticky-filter-input"><span class="text-sm font-bold text-slate-700">Very Good 8+</span></label>
          </div>
        </div>
      </div>

      <!-- AREA PILL -->
      <div class="relative pill-container shrink-0">
        <button type="button" class="pill-trigger shrink-0 flex items-center gap-1.5 px-4 py-2 rounded-full border border-slate-200 bg-white text-[13px] font-bold text-slate-700 transition-all active:scale-95 hover:border-slate-400 cursor-pointer whitespace-nowrap outline-none" data-target="areaBox">
          <svg class="w-3.5 h-3.5 text-blue-600" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2C8.1 2 5 5.1 5 9c0 5.2 7 13 7 13s7-7.8 7-13c0-3.9-3.1-7-7-7z"/></svg>
          <span>Area</span>
          <svg class="w-3.5 h-3.5 text-slate-400 transition-transform arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M19 9l-7 7-7-7" stroke-width="2.5" stroke-linecap="round"/></svg>
        </button>
        <div id="areaBox" class="filter-popup hidden absolute top-[calc(100%+8px)] left-0 w-[220px] bg-white border border-slate-200 rounded-2xl shadow-[0_10px_40px_rgba(0,0,0,0.15)] p-5 z-[1000]">
          <h4 class="text-[11px] font-black uppercase text-slate-400 mb-3 tracking-widest">Neighborhood</h4>
          <div class="space-y-3">
             <label class="flex items-center justify-between cursor-pointer group">
               <span class="text-sm font-bold text-slate-700 group-hover:text-blue-600">All Gulshan</span>
               <input type="radio" name="area" value="all" checked class="w-4 h-4 accent-blue-600 sticky-filter-input">
             </label>
             <label class="flex items-center justify-between cursor-pointer group">
               <span class="text-sm font-bold text-slate-700 group-hover:text-blue-600">Gulshan 1</span>
               <input type="radio" name="area" value="gulshan-1" class="w-4 h-4 accent-blue-600 sticky-filter-input">
             </label>
             <label class="flex items-center justify-between cursor-pointer group">
               <span class="text-sm font-bold text-slate-700 group-hover:text-blue-600">Gulshan 2</span>
               <input type="radio" name="area" value="gulshan-2" class="w-4 h-4 accent-blue-600 sticky-filter-input">
             </label>
          </div>
        </div>
      </div>

      <!-- DEALS (Instant Action) -->
      <button type="button" id="quickDealsBtn" class="shrink-0 flex items-center gap-1.5 px-4 py-2 rounded-full border border-red-100 bg-white text-[13px] font-[800] text-red-600 transition-all active:scale-95 hover:bg-red-50 cursor-pointer whitespace-nowrap outline-none">
        <svg class="w-3.5 h-3.5 fill-current" viewBox="0 0 24 24"><path d="M21.41 11.58l-9-9C12.05 2.22 11.55 2 11 2H4c-1.1 0-2 .9-2 2v7c0 .55.22 1.05.59 1.42l9 9c.36.36.86.58 1.41.58.55 0 1.05-.22 1.41-.59l7-7c.37-.36.59-.86.59-1.41 0-.55-.23-1.06-.59-1.42z"/></svg>
        <span>Deals</span>
      </button>

    </div>
  </div>
</header>

<style>
  .no-scrollbar::-webkit-scrollbar { display: none; }
  .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
  
  /* মোবাইলে পপআপকে বটম শিট স্টাইল করার জন্য */
  @media (max-width: 768px) {
    .filter-popup {
      position: fixed !important;
      top: auto !important;
      bottom: 0 !important;
      left: 0 !important;
      right: 0 !important;
      width: 100% !important;
      margin: 0 !important;
      border-radius: 24px 24px 0 0 !important;
      border-top: 1px solid #e2e8f0 !important;
      padding-bottom: 2.5rem !important;
      animation: slideIn 0.3s ease-out;
    }
  }

  @keyframes slideIn {
    from { transform: translateY(100%); }
    to { transform: translateY(0); }
  }
</style>

<script is:inline>
  function setupStickyBarLogic() {
    const hotelCards = document.querySelectorAll('.directory-card-item');
    const searchInput = document.getElementById('hotelSearchInput');
    const triggers = document.querySelectorAll('.pill-trigger');
    const popups = document.querySelectorAll('.filter-popup');
    const dealsBtn = document.getElementById('quickDealsBtn');
    let dealsActive = false;

    // মেইন ফিল্টারিং ফাংশন
    const runAllFilters = () => {
      const searchTerm = searchInput?.value.toLowerCase().trim() || "";
      
      // পপআপ থেকে ভ্যালু নেওয়া
      const selectedPrice = document.querySelector('input[name="price"]:checked')?.value || "all";
      const selectedArea = document.querySelector('input[name="area"]:checked')?.value || "all";
      const selectedRatings = Array.from(document.querySelectorAll('#ratingBox input:checked')).map(cb => parseFloat(cb.value));

      hotelCards.forEach(card => {
        const title = (card.getAttribute('data-title') || "").toLowerCase();
        const area = card.getAttribute('data-area') || "";
        const price = parseFloat(card.getAttribute('data-numeric-price') || "0");
        const rating = parseFloat(card.getAttribute('data-numeric-rating') || "0");
        const hasDiscount = card.querySelector('.bg-red-600') || card.querySelector('span[class*="red"]');

        // ১. সার্চ ম্যাচ
        const matchesSearch = searchTerm === "" || title.includes(searchTerm);
        
        // ২. এরিয়া ম্যাচ
        const matchesArea = selectedArea === "all" || area === selectedArea;
        
        // ৩. প্রাইস ম্যাচ (লজিক: Low < 50, High >= 50)
        let matchesPrice = true;
        if (selectedPrice === "low") matchesPrice = price < 50;
        else if (selectedPrice === "high") matchesPrice = price >= 50;

        // ৪. রেটিং ম্যাচ
        let matchesRating = true;
        if (selectedRatings.length > 0) {
          matchesRating = selectedRatings.some(r => rating >= r);
        }

        // ৫. ডিলস ম্যাচ
        let matchesDeals = true;
        if (dealsActive) matchesDeals = !!hasDiscount;

        // ফাইনাল ডিসিশন
        if (matchesSearch && matchesArea && matchesPrice && matchesRating && matchesDeals) {
          card.style.display = 'flex';
        } else {
          card.style.display = 'none';
        }
      });
    };

    // ইভেন্ট লিসেনার: পপআপের ভেতরের সব ইনপুট
    document.querySelectorAll('.sticky-filter-input').forEach(input => {
      input.addEventListener('change', runAllFilters);
    });

    // ইভেন্ট লিসেনার: সার্চ ইনপুট
    searchInput?.addEventListener('input', runAllFilters);

    // পপআপ টগল লজিক
    triggers.forEach(btn => {
      btn.onclick = (e) => {
        e.stopPropagation();
        const targetId = btn.getAttribute('data-target');
        const targetBox = document.getElementById(targetId);
        const isHidden = targetBox.classList.contains('hidden');

        popups.forEach(p => p.classList.add('hidden'));
        document.querySelectorAll('.arrow').forEach(a => a.classList.remove('rotate-180'));

        if (isHidden) {
          targetBox.classList.remove('hidden');
          btn.querySelector('.arrow')?.classList.add('rotate-180');
        }
      };
    });

    // বাইরে ক্লিক করলে বন্ধ
    document.addEventListener('click', (e) => {
      if (!e.target.closest('.pill-container')) {
        popups.forEach(p => p.classList.add('hidden'));
        document.querySelectorAll('.arrow').forEach(a => a.classList.remove('rotate-180'));
      }
    });

    // ডিলস বাটন
    if(dealsBtn) {
      dealsBtn.onclick = () => {
        dealsActive = !dealsActive;
        dealsBtn.classList.toggle('bg-red-600', dealsActive);
        dealsBtn.classList.toggle('text-white', dealsActive);
        runAllFilters();
      };
    }
  }

  // ইনিশিয়ালাইজ
  setupStickyBarLogic();
  document.addEventListener('astro:page-load', setupStickyBarLogic);
</script>
